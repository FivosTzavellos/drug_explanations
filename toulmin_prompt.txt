You build structured arguments using the Toulmin framework for drug efficacy claims.

We are testing this claim:
"$claim_text"

Below are multiple PubMed abstracts about this drug. Use ONLY information explicitly stated in the abstracts. Do not infer, assume, or add outside knowledge. If a detail is missing, leave it out rather than guessing.

$articles_block

1. Decide the overall stance of this body of evidence:
   - "supports" the claim,
   - "attacks" (contradicts) the claim,
   - or "irrelevant" to the claim (e.g., if most abstracts do not address the claim).
   If evidence is mixed, choose the dominant direction and capture nuance in the qualifier.

2. Extract ONE Toulmin-style argument that summarises the combined evidence:
   - Claim: one sentence capturing the overall conclusion about the claim.
   - Grounds: bullet list of key results across the abstracts (include sample sizes, groups, effect sizes, p-values when present, and PubMed IDs).
   - Warrant: one or two sentences on why the combined data support or attack the claim.
   - Backing: short notes on study design/quality (e.g. "randomised double-blind trial", "systematic review", "small open-label study").
   - Qualifier: how strong or conditional the overall conclusion is (e.g. "modest effect", "mixed evidence", "short-term", "specific population").
   - Rebuttals: limitations or exceptions mentioned (e.g. small sample, short follow-up, contradictory findings in specific subgroups).

Return a single JSON object with this structure:

{
  "stance": "supports" | "attacks" | "irrelevant",
  "toulmin": {
    "claim": "...",
    "data": ["...", "..."],
    "warrant": "...",
    "backing": ["..."],
    "qualifier": "...",
    "rebuttals": ["..."]
  }
}

The JSON must be valid and parseable.
